# ユーザーに年齢を入力させ、整数として返す。
# 入力が数字でない場合は 0 を返す関数を完成させてください。

def get_user_age() -> int:
 age_input = XXXXXXXXX("年齢を入力してください: ")
 if age_input.XXXXXXXXX():
  return XXXXXXXXX(age_input)
  return 0

# 実行例（ターミナル入力が必要）:
# print(get_user_age())