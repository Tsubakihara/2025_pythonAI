# 商品IDのリストから重複を除去して昇順に並べる関数を完成させてください。

def normalize_ids(product_ids: list[int]) -> list[int]:
 unique_ids = XXXXXXXXX(product_ids)
 sorted_ids = XXXXXXXXX(unique_ids)
 return sorted_ids

# 例:
# print(normalize_ids([3, 1, 2, 3, 2])) # [1, 2, 3]
